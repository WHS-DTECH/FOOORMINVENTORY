# Google OAuth Configuration
# Get these values from Google Cloud Console: https://console.cloud.google.com

# OAuth 2.0 Client ID (from credentials.json)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# OAuth 2.0 Client Secret (from credentials.json)
GOOGLE_CLIENT_SECRET=your-client-secret

# Authorized redirect URI (must match Google Cloud Console)
GOOGLE_REDIRECT_URI=http://localhost:5000/auth/callback

# Flask secret key (generate a secure random string)
FLASK_SECRET_KEY=your-secret-key-here

# Environment (development or production)
FLASK_ENV=development
